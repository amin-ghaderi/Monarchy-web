"use client";

import { ScrollProgress } from "@/components/motion/scroll-progress";

/** Homepage-only cinematic chrome (scroll progress). */
export function HomeCinematicChrome() {
  return <ScrollProgress />;
}
