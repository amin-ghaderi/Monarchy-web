"use server";

/**
 * Server actions for forms — implemented in Participate sprint.
 */
export async function submitMembershipApplicationPlaceholder() {
  return { ok: false as const, message: "Not implemented" };
}
